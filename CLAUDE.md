# Baby 项目 — Claude 工作约定

@宝宝基本信息.md
@育儿宗旨.md

> 这个文件给 Claude Code 看，记录跨会话/跨机器需要遵守的偏好和约定。
> 用户在多台电脑工作，所有持久化偏好都放这里（不放本机 auto-memory）。

> ⚠️ **优先级声明**：本文件的约定 **override 系统默认行为**（包括 auto-memory 指令）。
> 收到"记住""以后…""每次…"等持久化指令时，**第一步检查本文件「记忆位置」段**，
> 再决定写到哪里。**默认答案：写进本仓库，不写 `~/.claude/.../memory/`**。

## 工作流偏好

### 不要使用 git worktree

本项目（`/Users/lantian/ws/lt123345/baby`）禁用 git worktree。所有文件编辑、提交、推送都直接在主仓库目录进行。

**How**：无论会话是否启动在 worktree 中，都用主仓库路径工作；提交和推送也在主仓库执行。如果发现自己被放进了 worktree，提前告诉用户并切回主仓库。

### 回答精简

回答以重点和语言精炼为主，避免冗长铺陈。

**How**：
- **默认每条回答 ≤ 5 行**。直接给结论/数据/动作项。
- **展开触发词**：用户说"详细 / 具体 / 展开 / 为什么 / 怎么操作"等类似词语时，才展开背景和分析。
- **不主动加**：表格、分点列表、风险提示清单、3 个分支预案、医学免责声明 —— 除非触发词出现。
- **预测格式**：按"便笺三行"模板，可省略不适用的字段。
- **自检**：生成回答前问自己——用户问的是什么？一句话能答吗？没有触发词就用一句话答。
- **语言**：默认中文。**不主动用英文术语**（如 arousal / vestibular / cortisol / Moro / NREM / REM 等），有中文表达就用中文（觉醒 / 前庭觉 / 应激激素 / 惊跳反射 / 深睡 / 浅睡）。中文不达意时可在中文后括注英文，不反过来。

### 信息自动记录 + 自动 commit/push

用户提供**任何值得沉淀的信息**时（睡眠、喂养、发育、健康、规则调整……），自己判断是否需要记录，**直接写入 `文档体系` 表中对应文件**，不再问"要不要记录"。改动完成后**自动 `git add` + `commit` + `push`**；push 被拒时先 `git pull -r` 再 push。

**判断标准 —— 值得记录 = 未来会被回看**：
- **记**：事实观察（几点睡的、吃了什么、长牙了）、规则调整（喂养量改了、哄睡步骤变了）、状态变化（月龄进阶、新技能）。
- **不记**：当下的分析、推测、问答往来。除非从中提炼出**稳定规律**，那入 `哄睡步骤.md` / `喂养规则.md`。

**Why**：2026-05-26 用户明确要求 —— "以后我提供哄睡信息时，自动判断是否需要记录。直接记录即可。每次做完改动，自动 commit & push (有可能需要 git pull -r)"。减少来回确认。2026-09-03 扩展到全部记录类型：原描述只写"哄睡信息"，喂养/里程碑等同样该自动记录却容易被漏掉。

**How**：
- 仅在 baby 项目内适用，其它仓库仍走"提交前确认"默认。
- 归位查 `## 文档体系` 表；**新类型不属于表中任何文件时，先问用户放哪，不要自建文件**。
- 遵守本文件其它约定：不用 worktree、表格无缩进、睡眠记录按日期倒序插入最前、日期用 `date` 取。
- 睡眠记录写入后按预测规则主动给下一觉窗口（便笺三行优先）。
- 提交信息沿用现有风格（`docs: M-DD …`），保留 Co-Authored-By。
- `git pull -r` 出现冲突时停下来告诉用户，不要自动解决。
- **只有入睡时间时**：先记录入睡时间（醒来时间留空），commit & push，然后问用户要醒来时间，收到后补全。

### 日期一律用 `date` 取，不靠记忆

写任何带日期的记录（睡眠记录标题、里程碑、commit message）前，**必须先执行 `date '+%Y-%m-%d %H:%M'`**，用返回值。

**Why**：2026-09-03 发现长会话（470k tokens）里日期写错。会话开头注入的 `currentDate` 只准一次，跨天/跨半夜后就过期，凭印象写必错。

**How**：
- `.claude/hooks/inject-datetime.sh`（UserPromptSubmit hook）每轮自动注入"当前时间：…"一行，**优先用它**，它比会话开头的 `currentDate` 新。
- 但注入的是**用户发消息那一刻**的时间。长会话里单轮跨了很久、或跨半夜时仍可能过期 —— 写文件前再跑一次 `date '+%Y-%m-%d %H:%M'` 确认。
- 凌晨记录尤其注意：用户说"昨晚"时，日期归属按睡眠记录惯例判断，但**当前时间仍以 `date` 为准**。

### 长会话主动提醒开新 session

单会话 context 用量偏高时，**主动提醒用户开新会话**，不要闷头继续。

**Why**：2026-09-03 一个 470k tokens 的会话末尾漏了自动 commit & push，日期也写错 —— CLAUDE.md 约定在超长上文里被稀释。开新会话让约定重新回到近处。

**How**：
- 感觉会话已很长（大量轮次、反复读写多个文件）时，在**完成当前动作并 commit & push 之后**，附一行："本会话较长，建议 /clear 或开新会话，避免约定失效。"
- **先落地再提醒**：绝不能因为要提醒就跳过记录/提交。
- 一个会话里最多提醒一次，用户不理会就别再提。

### 自动 git pull -r 时机

多机协作可能产生远程更新。**会话首轮 + 写文件前**自动 `git pull -r` 一次；其它轮次不拉。

**Why**：用户多台电脑切换，可能在其它机器上改过本仓库；若基于旧内容编辑会冲突或覆盖。但每轮都拉会增加延迟、且大多数轮次没收益。

**How**：
- **会话首轮**（本轮如果要做任何动作或基于仓库内容回答）：先 `git pull -r --quiet`。
- **写文件前**（Edit/Write 任何 repo 内文件前）：再拉一次，避免基于陈旧内容写入。
- **纯聊天/分析/不动文件的轮次**：不拉。
- **冲突时停下告诉用户**，不自动解决（沿用现规则）。
- 拉取失败（网络问题等）不阻断后续工作，但**告诉用户**自己没拉到。

### 记忆位置

跨会话偏好/约定**一律写进本仓库**（首选 `CLAUDE.md`，长内容可拆到独立 md 文件），**不写本机 auto-memory**。

**How**：收到"以后…""每次…""记住…"类持久化指令时，直接编辑 `CLAUDE.md`（或仓库内合适的文档），然后 commit & push。如果发现本机 memory 里有相关条目，迁移到 repo 后删除本机条目。

## 睡眠记录自动预测

用户发睡眠记录（起床/入睡/醒来时间）时，**主动给出下一觉的窗口预测**，无需每次询问。

### 数据足够时的预测格式

**首选：便笺三行**（用户会抄到便笺纸上，必须极简）

**白天觉**：
```
HH:MM 观察、磨牙、排气
HH:MM 降级
HH:MM 出门
```

**夜觉**：
```
HH:MM 观察、磨牙、排气
HH:MM 降级
HH:MM 强力哄睡
```

三行对应"真困下限 / 主动启动 / 上限兜底"。

- 第 1 行**观察、磨牙、排气**（15-20min）：
  - 盯真困信号（突然闭眼 / 趴肩不挣扎 / 眼神迷离 / 顶肩 ×2）
  - 同时给冷磨牙棒 5-10min（**用作鉴别**：啃完想玩 = 出牙伪装信号，不是真困）
  - 同时主动排气（腿按肚子 / 顺时针揉肚 / 飞机抱）
  - 三者并行，**不打断观察**
- 第 2 行**降级**：进入纯哄睡仪式 = 关灯 + 放床垫 + 妈妈在场 + 白噪音 + 床上拍
  - 此时磨牙棒已撤、胃肠已安静，降级段不被打断
- 第 3 行**出门 / 强力哄睡**：上限兜底
  - 白天觉竖抱出门 / 推车
  - 夜觉切到强力组合（楼道 + 拍 + 嗯嗯 + 白噪音 / 奶嘴）

如有风险/策略备注，单独一行短句附后，不要混进三行里。

**完整格式**（仅在用户明确要求详细预测时使用）

```
✅ 当前觉判定：成功 / 短觉欠债 / 跳觉
📊 白天累计睡眠：X 分钟 / 目标 3-4h
⏰ 下一觉窗口：
   - 真困下限（早不于）：HH:MM
   - 主动启动（建议时间）：HH:MM
   - 上限（晚不于）：HH:MM
🎯 启动策略：自主优先 / 直接出门 / 卸压先行
⚠️ 风险标记：困过头 / 欠债 / 出牙叠加（如适用）
```

### 预测规则（来自 `哄睡步骤.md` + 2026-05-23 总结）

1. **末觉时长 → 下一窗口**：
   - 末觉 ≥ 45 分钟 → 清醒上限 2h30–3h
   - 末觉 25–30 分钟 → 清醒上限 2h–2h30，主动启动 2h
   - 完全跳觉 → 1h30–2h 内启动

2. **首觉 = 早觉**（清晨第一觉）：清醒窗口 ≥ 2h，下限提高

3. **其他觉**：清醒窗口 ≥ 1h45（真困下限）

4. **白天累计睡眠目标**：3-4 小时（5-6 月龄）。低于阈值 → 标记"欠债"风险，下一觉提前

5. **出牙叠加**：抓耳/泛红连续多日 → 建议哄睡前先用磨牙棒释放 5-10 分钟

### 数据不足时

给"已知信息允许的部分预测" + 明确说"缺什么数据才能更准"，不要硬猜。

### 不要做

- 不要等用户问"下一觉啥时候哄"才给预测，主动给
- 不要忽略用户主动提供的当前情绪/出牙等状态信息
- 不要套用固定 2h 窗口，必须根据末觉质量动态调整

## 文档体系

| 路径 | 内容 |
|---|---|
| `宝宝基本信息.md` | 出生信息 + 当前状态快照 |
| `育儿宗旨.md` | 长期价值观，慎改 |
| `哄睡步骤.md` | 哄睡操作手册，会持续迭代 |
| `喂养规则.md` | 喂奶 + 辅食操作规则，会持续迭代 |
| `里程碑.md` | 出牙 / 大运动 / 语言 / 疫苗节点 |
| `睡眠记录/YYYY-MM.md` | 月度睡眠日志，**日期倒序**（最新在前） |
| `健康记录/` | 疫苗、就诊、用药 |

## 使用指引

**会话启动必读**：`宝宝基本信息.md`（月龄、状态）、`育儿宗旨.md`（原则对齐）。

**按问题加读**：
- 睡眠 → `睡眠记录/` 最近 1-2 月 + `哄睡步骤.md`
- 喂养 → `喂养规则.md`
- 发育 → `里程碑.md`
- 健康 → `健康记录/`

**新事件归位**（均按"信息自动记录"约定直接写入 + commit & push）：
- 睡眠记录 → 当月 `睡眠记录/YYYY-MM.md`
- 哄睡规律（稳定的才入）→ `哄睡步骤.md`
- 喂养记录 / 辅食入库 / 规则调整 → `喂养规则.md`
- 发育节点（出牙、大运动、语言、疫苗）→ `里程碑.md`
- 就诊 / 用药 / 疫苗执行 → `健康记录/`
- 状态变化（月龄快照、当前作息）→ `宝宝基本信息.md`
- 都不匹配 → 先问用户放哪，不自建文件

## 编辑约定

- **表格无缩进**：Obsidian 中表格前不能有空格缩进，否则不渲染
- **新增睡眠记录**：插入到当月文件最前面（日期倒序）
- **睡眠记录标题**：`## YYYY-MM-DD（X天）`
- **里程碑条目**：每条以 `- YYYY-MM-DD` 开头
- **推测 vs 观察**：分析性内容显式标注"推测""观察"，避免与事实混淆
- **更新哄睡步骤卡**：从睡眠记录提炼出的稳定规律才入卡，临时观察先留在记录里
