# Baby 项目 — Claude 工作约定

> 这个文件给 Claude Code 看，记录跨会话/跨机器需要遵守的偏好和约定。
> 用户在多台电脑工作，所有持久化偏好都放这里（不放本机 auto-memory）。

## 工作流偏好

### 不要使用 git worktree

本项目（`/Users/lantian/ws/lt123345/baby`）禁用 git worktree。所有文件编辑、提交、推送都直接在主仓库目录进行。

**Why**：2026-05-22 发生过一次混乱 —— 会话在 worktree 中启动但用绝对路径写到主仓库，导致 worktree clean 而主仓库堆积未提交工作。用户表示这个项目用 worktree 没必要。

**How**：无论会话是否启动在 worktree 中，都用主仓库路径工作；提交和推送也在主仓库执行。如果发现自己被放进了 worktree，提前告诉用户并切回主仓库。

### 回答精简

回答以重点和语言精炼为主，避免冗长铺陈。

**Why**：用户偏好高信噪比沟通，长解释和过度小结浪费时间。

**How**：直接给结论/数据/动作项；只在用户明确要求或必要时展开背景；避免每次回答都套用完整模板（如预测格式可省略不适用的字段）。

## 睡眠记录自动预测

用户发睡眠记录（起床/入睡/醒来时间）时，**主动给出下一觉的窗口预测**，无需每次询问。

### 数据足够时的预测格式

**首选：便笺三行**（用户会抄到便笺纸上，必须极简）

```
HH:MM 观察
HH:MM 哄睡
HH:MM 出门
```

对应"真困下限 / 主动启动 / 上限兜底"。如有风险/策略备注，单独一行短句附后，不要混进三行里。

**完整格式**（仅在用户明确要求详细预测时使用）

```
✅ 当前觉判定：成功 / 短觉欠债 / 跳觉
📊 白天累计睡眠：X 分钟 / 目标 3-4h
⏰ 下一觉窗口：
   - 真困下限（早不于）：HH:MM
   - 主动启动（建议时间）：HH:MM
   - 上限（晚不于）：HH:MM
🎯 启动策略：自主优先 / 直接出门 / 卸压先行
⚠️ 风险标记：困过头 / 欠债 / 出牙叠加（如适用）
```

### 预测规则（来自 `哄睡步骤.md` + 2026-05-23 总结）

1. **末觉时长 → 下一窗口**：
   - 末觉 ≥ 45 分钟 → 清醒上限 2h30–3h
   - 末觉 25–30 分钟 → 清醒上限 2h–2h30，主动启动 2h
   - 完全跳觉 → 1h30–2h 内启动

2. **首觉 = 早觉**（清晨第一觉）：清醒窗口 ≥ 2h，下限提高

3. **其他觉**：清醒窗口 ≥ 1h45（真困下限）

4. **白天累计睡眠目标**：3-4 小时（5-6 月龄）。低于阈值 → 标记"欠债"风险，下一觉提前

5. **出牙叠加**：抓耳/泛红连续多日 → 建议哄睡前先用磨牙棒释放 5-10 分钟

### 数据不足时

给"已知信息允许的部分预测" + 明确说"缺什么数据才能更准"，不要硬猜。

### 不要做

- 不要等用户问"下一觉啥时候哄"才给预测，主动给
- 不要忽略用户主动提供的当前情绪/出牙等状态信息
- 不要套用固定 2h 窗口，必须根据末觉质量动态调整

## 文档体系

| 文件 | 用途 |
|---|---|
| `育儿宗旨.md` | 长期价值观，慎改 |
| `哄睡步骤.md` | 哄睡操作手册，会持续迭代 |
| `睡眠记录/YYYY-MM.md` | 每日睡眠记录，**日期倒序**（最新在前） |
| `健康记录/` | 健康事件 |
| `里程碑.md` | 发育里程碑 |
| `宝宝基本信息.md` | 基本信息 |

## 编辑约定

- **表格无缩进**：Obsidian 中表格前不能有空格缩进，否则不渲染
- **新增睡眠记录**：插入到当月文件最前面（日期倒序）
- **更新哄睡步骤卡**：从睡眠记录提炼出的稳定规律才入卡，临时观察先留在记录里
